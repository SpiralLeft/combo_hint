ComboHint session summary (for next session)
Date: 2026-03-23

1) Current repo state
- Modified: .gitignore, README.md, combo_hint.config.json, combo_hint.json, src/ComboHint.cs
- New folder/files: src/UI/ComboHintOverlay.cs, src/UI/ComboHintToggleButton.cs

2) Build/packaging status
- build.bat now calls powershell.exe with ExecutionPolicy Bypass explicitly.
- build.ps1 now builds combo_hint.csproj explicitly and copies deterministic output ComboHint.dll.
- Verified commands:
  - .\build.bat
  - powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1
- Note: direct .\build.ps1 may be blocked by local execution policy in some terminals.

3) UI architecture (latest)
- Overlay logic moved to src/UI/ComboHintOverlay.cs:
  - overlay lifecycle attach/dispose
  - hotkey toggle (F)
  - persisted visibility across turns
  - sync with End Turn button
  - state machine style show/hide animation
- Toggle button moved to src/UI/ComboHintToggleButton.cs:
  - class inherits NButton
  - end-turn-like visual tree, label style, hover/press feedback

4) Size/visual tuning entry points
- Overall button size follow end-turn size multiplier:
  - src/UI/ComboHintOverlay.cs -> ToggleButtonScale
- Inner texture size/shape:
  - src/UI/ComboHintToggleButton.cs -> Image offsets and Image scale
    - OffsetLeft/Top/Right/Bottom
    - _image.Scale

5) Runtime logging
- UI debug log file is written at mod root: combo_hint.ui.log
- Main tags include Overlay.RenderCheck / Overlay.ToggleButtonSync / State changes

6) Suggested next checks
- In-game validation for button visibility on turn transitions (player/enemy/player).
- Verify F hotkey toggle and button click keep persisted visibility behavior.
- If visual tuning continues, consider centralizing constants into one config block.
